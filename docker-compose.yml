version: "3.9"

services:
  gateway:
    image: ghcr.io/YOUR_GITHUB_USERNAME/demo-provenance-gateway:latest
    container_name: demo_gateway
    ports:
      - "9000:9000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./faiss_index_tpch:/app/faiss_index_tpch